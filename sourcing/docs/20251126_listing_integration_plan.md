# 出品連携機能 - 実装計画書

**作成日**: 2025-11-26
**対象プロジェクト**: ecauto - sourcing機能
**目的**: sourcing_candidatesから出品完了までの連携パイプライン構築

---

## 📋 概要

SellerSpriteから抽出した2034件のASIN候補を、実際の出品（listings）まで連携させる機能を実装する。

### 現状

- ✅ **sourcing_candidates**: 2034件のASIN候補（status='candidate'）
- ❌ **出品連携**: 未実装
- ✅ **出品システム**: scheduler/upload_executor は実装済み

### 目標

sourcing_candidates → products → upload_queue → listings (出品完了)

---

## 🔍 現状分析

### データベース構造

#### 1. sourcing.db（ASIN候補管理）
```sql
sourcing_candidates (
    asin TEXT UNIQUE,
    status TEXT DEFAULT 'candidate',  -- 'candidate', 'approved', 'rejected', 'imported'
    ...
)
```

#### 2. master.db（出品システム）
```sql
products (
    asin TEXT PRIMARY KEY,
    title_ja TEXT,
    amazon_price_jpy INTEGER,
    ...
)

upload_queue (
    asin TEXT,
    platform TEXT,
    account_id TEXT,
    scheduled_time TIMESTAMP,
    ...
)

listings (
    asin TEXT,
    platform TEXT,
    platform_item_id TEXT,
    status TEXT DEFAULT 'pending',  -- 'pending', 'queued', 'listed'
    ...
)
```

### 既存の出品パイプライン

```
products テーブル
  ↓
upload_queue テーブル
  ↓ (upload_executor が処理)
listings テーブル (status='listed')
```

### 未実装の部分

**sourcing_candidates → products/upload_queue への連携スクリプト**

---

## ⚠️ SP-APIレート制限の問題

### 課題

- **SP-API使用**: 商品情報取得に必要
- **レート制限**: 12秒/リクエスト (公式10秒 + 余裕2秒)
- **バッチ処理**: 20件/リクエスト
- **既存プロセス**: sync_inventory_daemon が1時間ごとに実行中

### 所要時間の計算

```
2034件 ÷ 20件/バッチ = 101.7 ≈ 102リクエスト
102リクエスト × 12秒 = 1224秒 ≈ 20分
```

### 対策

**オプションC: 手動実行（Phase 1）**
```
1. daemon停止確認
2. 出品連携スクリプト実行（約20分）
3. 出品完了確認
4. daemon再開
```

**将来的な改善（Phase 2）**
- 方式3: 共有SP-APIClient管理機構
- プロセス間通信による排他制御
- SP-APIリクエストキューイング

---

## 🎯 実装方針（Phase 1）

### アプローチ

**段階的実装**: まず動作確認 → その後、共有リソース管理を実装

### Phase 1: 手動実行による出品連携（今回）

**実装内容**:
1. daemon停止確認スクリプト
2. 出品連携スクリプト (`import_candidates_to_master.py`)
3. アカウント振り分け（1000件ずつランダム - 仮実装）
4. 動作確認

**想定実行フロー**:
```bash
# 1. daemon停止確認
python check_daemon_status.py

# 2. daemon停止（必要に応じて）
# Windowsサービスマネージャーから停止

# 3. 出品連携実行
python sourcing/scripts/import_candidates_to_master.py \
  --limit 2000 \
  --dry-run false

# 4. daemon再開
```

### Phase 2: 共有SP-APIClient管理（将来）

**実装内容**:
- プロセスロックファイル方式
- SP-APIリクエストキュー
- 優先度管理
- 時間帯制限（オプション）

---

## 📝 実装タスク

### タスク1: daemon停止確認スクリプト (30分)

**ファイル**: `check_daemon_status.py` (既存を確認・必要なら拡張)

**機能**:
- sync_inventory_daemon の実行状態確認
- ロックファイルチェック
- 安全な実行タイミングの判断

### タスク2: アカウント情報確認 (15分)

**確認項目**:
- 利用可能なプラットフォーム・アカウント一覧
- 各アカウントの出品可能件数
- アカウント割り振りロジックの確認

**実行**:
```bash
python -c "from platforms.base.accounts.manager import AccountManager; \
           am = AccountManager(); \
           accounts = am.get_all_accounts(); \
           print(accounts)"
```

### タスク3: 出品連携スクリプト実装 (2-3時間)

**ファイル**: `sourcing/scripts/import_candidates_to_master.py`

**機能**:
```python
class CandidateImporter:
    """
    sourcing_candidatesからmaster.dbへの連携
    """

    def __init__(self, limit: int = None, dry_run: bool = False):
        self.sourcing_db = Path('sourcing/data/sourcing.db')
        self.master_db = MasterDB()
        self.sp_api_client = AmazonSPAPIClient(SP_API_CREDENTIALS)
        self.queue_manager = UploadQueueManager()
        self.limit = limit
        self.dry_run = dry_run

    def run(self):
        """メイン処理"""
        # 1. sourcing_candidatesから未処理ASIN取得
        asins = self._get_candidate_asins()

        # 2. SP-APIで商品情報取得 (バッチ20件ずつ)
        for batch in self._batch_asins(asins, 20):
            products_data = self.sp_api_client.get_products_batch(batch)

            # 3. productsテーブルに登録
            for asin, data in products_data.items():
                self.master_db.upsert_product(asin, data)

        # 4. アカウント割り振り (1000件ずつランダム)
        account_assignments = self._assign_accounts(asins)

        # 5. upload_queueに追加
        for asin, account_id in account_assignments.items():
            self.queue_manager.add_to_queue(
                asin=asin,
                platform='base',
                account_id=account_id,
                priority=UploadQueueManager.PRIORITY_NORMAL
            )

        # 6. sourcing_candidatesのstatus更新
        self._update_candidate_status(asins, 'imported')
```

**主要メソッド**:
- `_get_candidate_asins()`: sourcing.dbからASIN取得
- `_batch_asins()`: バッチ分割（20件ずつ）
- `_assign_accounts()`: アカウント割り振り（1000件ずつランダム）
- `_update_candidate_status()`: statusを'imported'に更新

### タスク4: 動作確認テスト (1時間)

**テストシナリオ**:
```bash
# テスト1: Dry Run（10件）
python sourcing/scripts/import_candidates_to_master.py \
  --limit 10 \
  --dry-run true

# テスト2: 実行（100件）
python sourcing/scripts/import_candidates_to_master.py \
  --limit 100 \
  --dry-run false

# テスト3: 全件実行（2000件）
python sourcing/scripts/import_candidates_to_master.py \
  --limit 2000 \
  --dry-run false
```

**検証項目**:
- ✅ SP-APIレート制限が正常に機能
- ✅ productsテーブルに商品情報が登録
- ✅ upload_queueに正しく追加
- ✅ アカウント割り振りが均等
- ✅ sourcing_candidatesのstatus更新
- ✅ upload_executor による出品完了確認

### タスク5: 進捗レポート更新 (30分)

**ファイル**: このドキュメント（20251126_listing_integration_plan.md）

**更新内容**:
- 実装完了状況
- テスト結果
- 遭遇した問題と解決策
- 次のステップ

---

## 🔄 出品パイプライン（完成形）

```
┌─────────────────────────────────────────────────────────┐
│ ASIN抽出 (Phase 1完了)                                   │
│ - extract_asins_bulk.py                                 │
│ - 2034件の候補ASIN取得                                   │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│ sourcing_candidates (sourcing.db)                       │
│ - status='candidate'                                    │
│ - 2034件                                                │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│ 出品連携 (Phase 1 - 今回実装)                             │
│ - import_candidates_to_master.py                        │
│ - SP-APIで商品情報取得 (約20分)                          │
│ - アカウント割り振り (1000件ずつ)                         │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│ products (master.db)                                    │
│ - 商品マスタ                                             │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│ upload_queue (master.db)                                │
│ - 出品キュー                                             │
│ - scheduled_time自動設定                                 │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│ 出品実行 (既存システム)                                   │
│ - upload_executor.py                                    │
│ - BASE APIで出品                                         │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│ listings (master.db)                                    │
│ - status='listed'                                       │
│ - 出品完了                                               │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│ sourcing_candidates更新                                  │
│ - status='imported'                                     │
└─────────────────────────────────────────────────────────┘
```

---

## ✅ 成功基準

### Phase 1完了の条件

- ✅ daemon停止確認スクリプト動作
- ✅ import_candidates_to_master.py 実装完了
- ✅ 2000件のASINが正常に出品キューに追加
- ✅ アカウント割り振りが均等（1000件ずつ）
- ✅ upload_executorによる出品完了確認
- ✅ sourcing_candidatesのstatus='imported'確認
- ✅ SP-APIレート制限エラーなし

### 品質基準

- エラーハンドリング: 個別ASINエラーでも処理継続
- ログ出力: 進捗状況の可視化
- Dry Runモード: テスト実行可能
- ロールバック: エラー時の復旧手順明確化

---

## 📊 実装スケジュール

| タスク | 所要時間 | 優先度 |
|--------|---------|-------|
| daemon停止確認 | 30分 | 高 |
| アカウント情報確認 | 15分 | 高 |
| 出品連携スクリプト実装 | 2-3時間 | 最高 |
| 動作確認テスト | 1時間 | 高 |
| 進捗レポート更新 | 30分 | 中 |

**合計**: 約4-5時間

---

## 🚀 次のステップ

### 今回（Phase 1）

1. daemon停止確認
2. アカウント情報確認
3. import_candidates_to_master.py 実装
4. テスト実行（10件 → 100件 → 2000件）
5. 出品完了確認

### 将来（Phase 2）

1. 共有SP-APIClient管理機構の設計
2. プロセス間排他制御の実装
3. SP-APIリクエストキューイング
4. 優先度管理の実装
5. 自動実行化（daemon統合）

---

## 📚 関連ドキュメント

- [Phase 1完了レポート](20251125_implementation_progress_report_v3.md)
- [SellerSprite抽出機能](../sources/sellersprite/USAGE.md)
- [出品システム](../../scheduler/README.md)
- [SP-APIレート制限対応](../../docs/issues/ISSUE_006_sp_api_rate_limit_getpricing_migration.md)

---

**作成**: 2025-11-26
**Phase 1開始**: 2025-11-26
**Phase 1完了予定**: 2025-11-26（同日完了想定）
